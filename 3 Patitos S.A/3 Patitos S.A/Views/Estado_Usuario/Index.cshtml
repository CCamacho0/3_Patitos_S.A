@model IEnumerable<_3_Patitos_S.A.Models.Estado_Usuario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre_estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre_estado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_estado_usuario">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_estado_usuario">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_estado_usuario">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
